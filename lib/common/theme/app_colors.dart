import 'package:flutter/material.dart';

class AppColors {
  // Main
  static const Color primary = Color(0xFFc1ae89);
  static const Color secondary = Color(0xFFFFFFFF);
  static const Color background = Color(0xFF060607);

  static const Color backgroundCardDark = Color(0xFF1c1c1d);
  static const Color backgroundCard = Color(0xFF2b2b2b);
  static const Color backgroundCardLight = Color(0xFF34343c);
  static const Color backgroundLight = Color(0xFF1c1c1d);

  // Text
  static const Color text = Colors.white;
  static const Color textComplementary = Colors.black;

  // Action
  static const Color error = Colors.redAccent;
  static const Color success = Color(0xFF20AD70);
  static const Color disabled = Color(0xFF848484);

  static const Color selected = primary;
  static const Color unselected = secondary;
}
